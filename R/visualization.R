#' @title Make a hit calling plot
#' @description Show both the
hit_calling_plot = function(protein, strain,
                            bead_binding,
                            concordances){

  both_plot = a + b
  plot_list = list(a = interval_plot,
                   b = entropy plot,
                   ab = both_plot)
  return(plot_list)
}
